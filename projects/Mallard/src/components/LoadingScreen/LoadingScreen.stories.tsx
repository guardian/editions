import { storiesOf } from '@storybook/react-native';
import React from 'react';
import { LoadingScreen } from './LoadingScreen';

storiesOf('Loading Screen', module).add('default', () => <LoadingScreen />);
