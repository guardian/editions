import React from 'react'
import { Multiline } from 'src/components/multiline'
import { color } from 'src/theme/color'

const ArticleMultiline = () => {
    return <Multiline count={4} color={color.line} />
}

export { ArticleMultiline }
